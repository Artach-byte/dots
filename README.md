# Reqs for neovim
 - On Ubuntu

  ```
  sudo apt install xsel
  ```
  - On Arch Linux

  ```
  sudo pacman -S xsel
  ```
 - Neovim python support

  ```
  pip install pynvim
  ```
  - Neovim node support

  ```
  npm i -g neovim
  ```
  - ncdu
  - lazygit
  - htop
---
