# Aliases for LSD
alias ls='lsd'
alias l='ls -l'
alias la='ls -a'
alias lla='ls -la'
alias lt='ls --tree'

# Aliases for ZSH
alias zshreload='source ~/.zshrc'             # reload ZSH

# lazygit
alias lg='lazygit'

# back directory
alias ..='cd ..'

# Ranger
alias r='ranger'
